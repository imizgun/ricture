[package]
name = "ricture"
version = "0.1.0"
edition = "2024"

[dependencies]
ricture-capture = { path = "../capture" }
ricture-overlay = { path = "../overlay" }
ricture-config = {path = "../config" }
chrono = "0.4.45"